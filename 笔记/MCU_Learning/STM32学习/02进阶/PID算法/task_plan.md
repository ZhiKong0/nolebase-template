# 421 循迹中线钳制与静态偏置修复计划

## 目标

解决 `Project_Refactor_track-421 - 副本 (2)` 在 `TRACK` 模式下直线段容易蛇形、`S4/S5` 中线钳制不稳的问题，并把必要的静态差速偏置纳入正式调参链。

## 阶段

- [completed] 1. 核对当前循迹控制链与日志指标
- [completed] 2. 重构中心区误差建模与静态偏置链
- [completed] 3. 编译并校验串口调参接口
- [completed] 4. 烧录并做在线调参复测
- [completed] 5. 固化当前最稳默认参数并准备提交 Git

## 当前结论

- 主根因不是单纯 `KP/KD` 不够，而是原实现把 `S4` 单灯和 `S5` 单灯也压成了 `0` 偏差，导致中心区误差不可观测。
- 小车存在轻微固定单边趋势，但幅度不大；需要小量静态差速偏置，而不是大偏置硬拧。
- 当前已把中心区单灯误差恢复、中心双灯锁定收窄，并把 `TRACK` 静态偏置接入串口与 PC 调参链。
