# Archived Sessions

Closed sessions move here after release.

Archive by date partition so that recent and historical sessions remain easy to inspect without loading everything.

Each archived session folder keeps:

- `SESSION_DELTA.json`
- `THOUGHT.jsonl`
- `EXEC.jsonl`
- `MOD.jsonl`
