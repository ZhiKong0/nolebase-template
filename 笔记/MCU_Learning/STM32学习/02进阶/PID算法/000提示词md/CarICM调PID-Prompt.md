- 串口数据是我把车放地上之后开始测试，测试是通过py程序或者cmd去对串口发送命令，最后实现让小车按照规则动起来跑直线，然后跑完后实时查看串口传到电脑的数据并进行分析，分析出小车的运行情况，并和我在实物方向所看到的现象对齐颗粒度，之后进行后面的修改建议和下一步测试，
- 要求对于能修改的数值我希望在串口传数据发送数据的时候设置成可调数据，这样可以一步一步逼近我想要的pid值，并且这样的可调数据尽可能量程大或者分辨率高方便详细的精准的去调pid
- “10 秒以上实验 + 实时看更细粒度串口数据 + 参数尽可能高分辨率可调”拆成两条链路同时满足：
- 链路A（实时观测）：在实验进行中持续输出短帧高频数据（避免 115200 下长文本丢包），让你能边看车边对齐现象颗粒度；必要时用 VOFA 的 #BIN=1! 看 3 通道波形。
- 链路B（可复现分析）：实验结束后仍然保留 #EXP=DUMP 的 CSV 数据，但因为 STM32F103C8 SRAM 很紧张，10s 以上无法缓存 10ms*全量字段，因此我会继续采用“自动降采样缓存 + 实时流式输出补全细节”的组合方案。
- 然后我希望你能够通过py实现算法对这个串口传出来的的每一轮数据进行数据分析，可以分析出对于下一步有效的可参考的数据@analyze_last_dump.py 类似于这样的脚本的行为，但是我希望更加细致一点，更精密一点，实现小车运动状态的全方位捕捉和小车调参的精细化调整，可以写这样的算法类的py程序的时候可以考虑使用更适合计算的库



- 请你自动化开始测试，开是exp01测试，测试之后py文件提取得到的csv文件存在@000Data 中，并能够分析，最后给出建议，针对exp2，你给出怎么样的参数，然后问我是否进行exp2
