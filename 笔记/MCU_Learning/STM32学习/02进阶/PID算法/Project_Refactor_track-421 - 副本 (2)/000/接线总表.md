# 三轮小车接线总表（STM32F103C8T6 + TB6612 + MG310(GMR) + ICM42688 / BNO085 + OLED）

> 本表按当前工程代码与接线方案整理：默认 IMU 为 `ICM42688`，使用 `PB12/PB13` 软件 I2C；若后续改用 `BNO085`，也建议优先复用这组 I2C 引脚。

## 1. 电源与地

| 模块端                 | 连接到                  | 说明                |
| ---------------------- | ----------------------- | ------------------- |
| 电池正极               | TB6612 `VM`           | 电机电源（如 7.4V） |
| 电池正极               | 3.3V稳压模块 `VIN+`   | 给逻辑电源降压      |
| 3.3V稳压模块 `VOUT+` | STM32 `3.3V`          | 主控供电            |
| 3.3V稳压模块 `VOUT+` | TB6612 `VCC`          | 驱动逻辑供电        |
| 电池负极               | STM32 `GND`           | 共地                |
| 电池负极               | TB6612 `GND`          | 共地                |
| 电池负极               | 编码器/MPU/OLED `GND` | 全系统共地          |

## 2. TB6612 ↔ STM32

| TB6612 引脚 | STM32 引脚         | 用途         |
| ----------- | ------------------ | ------------ |
| `STBY`    | `PB0`            | 驱动使能     |
| `PWMA`    | `PA8 (TIM1_CH1)` | 左电机 PWM   |
| `AIN1`    | `PA4`            | 左电机方向 1 |
| `AIN2`    | `PA5`            | 左电机方向 2 |
| `PWMB`    | `PA9 (TIM1_CH2)` | 右电机 PWM   |
| `BIN1`    | `PB1`            | 右电机方向 1 |
| `BIN2`    | `PB10`           | 右电机方向 2 |

## 3. 电机与编码器（MG310）

### 3.1 左轮（电机 A）

| MG310-A 引脚        | 连接到             | 说明          |
| ------------------- | ------------------ | ------------- |
| `1 (Motor+)`      | TB6612 `AO1`     | 电机线        |
| `6 (Motor-)`      | TB6612 `AO2`     | 电机线        |
| `5 (Encoder VCC)` | STM32 `3.3V`     | 编码器供电    |
| `2 (Encoder GND)` | STM32 `GND`      | 编码器地      |
| `4 (Encoder A)`   | `PA0 (TIM2_CH1)` | 左编码器 A 相 |
| `3 (Encoder B)`   | `PA1 (TIM2_CH2)` | 左编码器 B 相 |

### 3.2 右轮（电机 B）

| MG310-B 引脚        | 连接到             | 说明          |
| ------------------- | ------------------ | ------------- |
| `1 (Motor+)`      | TB6612 `BO1`     | 电机线        |
| `6 (Motor-)`      | TB6612 `BO2`     | 电机线        |
| `5 (Encoder VCC)` | STM32 `3.3V`     | 编码器供电    |
| `2 (Encoder GND)` | STM32 `GND`      | 编码器地      |
| `4 (Encoder A)`   | `PA6 (TIM3_CH1)` | 右编码器 A 相 |
| `3 (Encoder B)`   | `PA7 (TIM3_CH2)` | 右编码器 B 相 |

## 4. ICM42688（软件 I2C）

| ICM42688 引脚 | STM32 引脚     | 说明                                                 |
| ------------- | -------------- | ---------------------------------------------------- |
| `VCC`       | `3.3V`       | 模块供电（以模块规格为准，推荐 3.3V）                |
| `GND`       | `GND`        | 共地                                                 |
| `SCL/SCLK`  | `PB12`       | 软件 I2C SCL                                         |
| `SDA/MOSI`  | `PB13`       | 软件 I2C SDA                                         |
| `ADO/MISO`  | `GND`        | I2C 地址选择：接 GND ->`0x68`，接 3.3V -> `0x69` |
| `CS`        | （悬空或接高） | I2C 模式不用（SPI 模式低有效）                       |
| `INTT`      | （可不接）     | 中断输出1（可接 EXTI）                               |
| `INT2`      | （可不接）     | 中断输出2（可选）                                    |
| `3V3`       | NC             | 3.3V 稳压输出（可不接）                              |
| `NC`        | NC             | 悬空                                                 |

> 建议：`PB12/PB13` 各接一个 4.7k 上拉到 3.3V（若模块板载上拉，可先不外接）。

### 4.1 BNO085（推荐 I2C，作为 ICM42688 替换方案）

| BNO085 引脚 | STM32 引脚（推荐）       | 说明                                   |
| ----------- | ------------------------ | -------------------------------------- |
| `VDD`     | `3.3V`                 | 模块供电，严禁直接接 5V                |
| `GND`     | `GND`                  | 共地                                   |
| `SCL`     | `PB12`                 | 推荐复用当前 IMU 的 I2C 时钟脚         |
| `SDA`     | `PB13`                 | 推荐复用当前 IMU 的 I2C 数据脚         |
| `INT`     | `PA15` 或任意空闲 GPIO | 数据就绪中断，建议连接                 |
| `RST`     | `PB14` 或任意空闲 GPIO | 软件复位，建议接可控 GPIO              |
| `ADDR`    | `GND` 或 `3.3V`      | 地址选择：`GND -> 0x28`，`3.3V -> 0x29` |
| `PS0`     | `GND`                  | I2C 模式选择                           |
| `PS1`     | `3.3V`                 | I2C 模式选择                           |

> 推荐替换方式：直接把原 `ICM42688` 的 `PB12/PB13` 改接到 `BNO085` 的 `SCL/SDA`，这样改线最少。
>
> 注意：如果你仍保留当前蓝牙接线，则不要把 `PB14` 固定给 `BNO085_RST`，因为 `PB14/PB15` 目前在总表中预留给软件串口蓝牙，`RST` 可以改接其他空闲 GPIO。

## 5. OLED（软件 I2C）

| OLED 引脚 | STM32 引脚 | 说明         |
| --------- | ---------- | ------------ |
| `VCC`   | `3.3V`   | 供电         |
| `GND`   | `GND`    | 地           |
| `SCL`   | `PB7`    | 软件 I2C SCL |
| `SDA`   | `PB8`    | 软件 I2C SDA |

## 6. 串口调参（USART2）

| 功能          | STM32 引脚 | 连接对象        |
| ------------- | ---------- | --------------- |
| `USART2_TX` | `PA2`    | USB-TTL `RX`  |
| `USART2_RX` | `PA3`    | USB-TTL `TX`  |
| `GND`       | `GND`    | USB-TTL `GND` |

## 7. STM32 引脚占用总览

| STM32 引脚 | 信号/模块                    |
| ---------- | ---------------------------- |
| `PA0`    | 编码器左 A（TIM2_CH1）       |
| `PA1`    | 编码器左 B（TIM2_CH2）       |
| `PA2`    | USART2_TX（USB-TTL/VOFA+）   |
| `PA3`    | USART2_RX（USB-TTL/VOFA+）   |
| `PA4`    | AIN1                         |
| `PA5`    | AIN2                         |
| `PA6`    | 编码器右 A（TIM3_CH1）       |
| `PA7`    | 编码器右 B（TIM3_CH2）       |
| `PA8`    | PWMA（TIM1_CH1）             |
| `PA9`    | PWMB（TIM1_CH2）             |
| `PA10`   | 八路循迹 `S1`（推荐示例）    |
| `PA11`   | 八路循迹 `S2`（推荐示例）    |
| `PA12`   | 八路循迹 `S3`（推荐示例）    |
| `PA15`   | 可选 `BNO085_INT`（替换 IMU 时） |
| `PB0`    | STBY                         |
| `PB1`    | BIN1                         |
| `PB3`    | 八路循迹 `S4`（推荐示例，需释放 JTAG） |
| `PB4`    | 八路循迹 `S5`（推荐示例，需释放 JTAG） |
| `PB5`    | 按键1（启动/停止）           |
| `PB6`    | 按键2（切换速度）            |
| `PB7`    | OLED_SCL                     |
| `PB8`    | OLED_SDA                     |
| `PB9`    | 八路循迹 `S6`（推荐示例）    |
| `PB10`   | BIN2                         |
| `PB11`   | 八路循迹 `S7`（推荐示例）    |
| `PB12`   | ICM42688_SCL / BNO085_SCL    |
| `PB13`   | ICM42688_SDA / BNO085_SDA    |
| `PB14`   | 软件串口 TX（蓝牙）/ 可选 `BNO085_RST` |
| `PB15`   | 软件串口 RX（蓝牙 HC-05/06） |
| `PC13`   | 八路循迹 `S8`（推荐示例）    |

## 8. 按键模块

| 按键     | STM32 引脚 | 功能                          |
| -------- | ---------- | ----------------------------- |
| `按键` | `PB5`    | 短按=启动/停止，长按=切换速度 |

**接线方式（上拉输入模式）：**

```
按键: 一端接PB5，另一端接GND

短按（<1秒）= 启动/停止电机
长按（≥1秒）= 切换速度档位

按下时 = 低电平（触发）
松开时 = 高电平（内部上拉）
```

## 9. 蓝牙模块（HC-05/HC-06）

| HC-05/HC-06 引脚 | STM32 引脚 | 说明                               |
| ---------------- | ---------- | ---------------------------------- |
| `VCC`          | `3.3V`   | 供电（HC-06支持3.3V，HC-05建议5V） |
| `GND`          | `GND`    | 共地                               |
| `TXD`          | `PB15`   | 软件串口 RX                        |
| `RXD`          | `PB14`   | 软件串口 TX                        |

> **注意**：PB14/PB15 原为 JTAG 引脚，已在代码中禁用 JTAG 释放这两个引脚。

## 10. 八路循迹模块（推荐用 `S1~S8` 并口直读）

| 八路循迹模块引脚 | STM32 引脚（推荐示例） | 说明 |
| ---------------- | ---------------------- | ---- |
| `5V`           | `5V`                 | 模块供电，按模块丝印接 5V |
| `GND`          | `GND`                | 必须共地 |
| `S1`           | `PA10`               | 数字输入 1 |
| `S2`           | `PA11`               | 数字输入 2 |
| `S3`           | `PA12`               | 数字输入 3 |
| `S4`           | `PB3`                | 数字输入 4 |
| `S5`           | `PB4`                | 数字输入 5 |
| `S6`           | `PB9`                | 数字输入 6 |
| `S7`           | `PB11`               | 数字输入 7 |
| `S8`           | `PC13`               | 数字输入 8 |
| `TX`           | 暂不接               | 仅在使用 UART 模式时再接 |
| `RX`           | 暂不接               | 仅在使用 UART 模式时再接 |
| `SDA`          | 暂不接               | 仅在使用 I2C 模式时再接 |
| `SCL`          | 暂不接               | 仅在使用 I2C 模式时再接 |

### 10.1 使用建议

- **[优先方案]** 先使用 `S1~S8` 直连 STM32 GPIO 的并口模式，最容易调试每一路压线状态
- **[供电]** 模块丝印为 `5V` 时，供电按 `5V` 接；但进入 STM32 的信号电平要确认是否兼容 `3.3V`
- **[备用接口]** `TX/RX/SDA/SCL` 通常是模块的备用通信方式，不建议和 `S1~S8` 首轮调试时一起接
- **[JTAG 影响]** 若使用 `PB3/PB4`，需要确认工程已释放对应 JTAG 复用；若你不想占用这些脚，可把 `S4/S5` 改接到其它空闲 GPIO

## 11. 上电前检查

| 检查项    | 标准                                                                    |
| --------- | ----------------------------------------------------------------------- |
| 共地      | 电池负极、STM32 GND、TB6612 GND、编码器 GND、ICM/BNO GND、OLED GND 全部连通 |
| 逻辑电压  | STM32/TB6612 VCC/编码器/ICM/BNO/OLED 都是 3.3V                              |
| STBY 逻辑 | `PB0=0` 电机不转，`PB0=1` 才允许驱动                                |
| 串口线序  | PA2→TTL-RX，PA3→TTL-TX，不能接反                                      |
| BNO085 模式脚 | 若使用 BNO085 的 I2C，推荐 `PS0=0`、`PS1=1`，且 `ADDR` 不要悬空        |
| 八路循迹供电 | 若模块丝印为 `5V`，则模块供电接 `5V`，不要误接 `3.3V`                     |
| 八路循迹电平 | 若使用 `S1~S8` 直读，先确认模块输出高电平是否兼容 STM32 `3.3V` 输入        |
