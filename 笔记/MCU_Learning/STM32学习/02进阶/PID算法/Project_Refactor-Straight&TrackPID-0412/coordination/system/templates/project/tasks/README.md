# Tasks

Put one Markdown file here per task.

Recommended rules:

- one task id per file
- one active worktree per active coding task
- one active session delta per active coding task
- treat the task file as live working memory, not a final report
- keep it current enough that another AI can resume without your chat history
